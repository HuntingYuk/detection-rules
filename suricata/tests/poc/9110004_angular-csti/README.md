# SID 9110004 — WEB XSS client-side template injection (AngularJS constructor.constructor) in http.uri

> Auto-generated by `scripts/gen-docs` from `tests/poc/_descriptions.json` + rule metadata. **Jangan edit di sini** — edit JSON-nya lalu jalankan `scripts/gen-docs`.

**Rule:** `rules/plaintext/xss.rules` · **Confidence:** medium · **OWASP:** A03-Injection · **Severity:** Major · **Network:** plain

## Serangan apa ini?
Client-Side Template Injection (AngularJS sandbox-escape): payload `{{constructor.constructor('...')()}}` direfleksikan & dieksekusi di browser.

## Kalau kena, dampaknya?
Eksekusi JavaScript di browser korban (curi sesi / aksi atas nama korban) — meski input dipantulkan sebagai 'template', bukan HTML mentah.

## Apa yang rule ini deteksi?
Gadget `constructor.constructor(` di URL; threshold 3/60 detik kompensator FP.

## PoC vs Negative
- `poc.sh` → **harus** memicu SID 9110004.
- `negative.sh` → lookalike jinak; kena fast_pattern lalu **ditolak pcre** → TIDAK alert (§19 T6).

## Cara jalanin
```
scripts/rulectl check 9110004
```
