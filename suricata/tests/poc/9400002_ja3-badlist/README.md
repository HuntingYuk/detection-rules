# SID 9400002 — TLS anomaly - client JA3 on curated bad-list

> Auto-generated by `scripts/gen-docs` from `tests/poc/_descriptions.json` + rule metadata. **Jangan edit di sini** — edit JSON-nya lalu jalankan `scripts/gen-docs`.

**Rule:** `rules/tls-meta/tls-anomaly.rules` · **Confidence:** medium · **OWASP:** none · **Severity:** Major · **Network:** tls

## Serangan apa ini?
TLS anomaly: JA3 fingerprint klien cocok dengan daftar-buruk terkurasi (tanpa dekripsi).

## Kalau kena, dampaknya?
Deteksi tool/malware berdasarkan sidik perilaku TLS, bukan IP.

## Apa yang rule ini deteksi?
Hash JA3 (dihitung dari ClientHello cleartext) ada di dataset badja3 (entri ber-TTL/provenance).

## PoC vs Negative
- `poc.sh` → **harus** memicu SID 9400002.
- `negative.sh` → lookalike jinak; kena fast_pattern lalu **ditolak pcre** → TIDAK alert (§19 T6).

## Cara jalanin
```
scripts/rulectl check 9400002
```
