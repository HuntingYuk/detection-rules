# SID 9420002 — RECON ICMP echo sweep - high echo-request rate from one source

> Auto-generated by `scripts/gen-docs` from `tests/poc/_descriptions.json` + rule metadata. **Jangan edit di sini** — edit JSON-nya lalu jalankan `scripts/gen-docs`.

**Rule:** `rules/tls-meta/l34-recon.rules` · **Confidence:** medium · **OWASP:** none · **Severity:** Minor · **Network:** any

## Serangan apa ini?
Recon: ICMP echo sweep — laju echo-request tinggi dari satu sumber (penemuan host).

## Kalau kena, dampaknya?
Pemetaan host hidup di jaringan.

## Apa yang rule ini deteksi?
Laju ICMP echo-request dari satu src melebihi threshold (non-payload L3/L4).

## PoC vs Negative
- `poc.sh` → **harus** memicu SID 9420002.
- `negative.sh` → lookalike jinak; kena fast_pattern lalu **ditolak pcre** → TIDAK alert (§19 T6).

## Cara jalanin
```
scripts/rulectl check 9420002
```
