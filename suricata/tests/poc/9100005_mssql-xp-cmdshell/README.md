# SID 9100005 — WEB SQLi MSSQL xp_cmdshell OS command execution in http.uri

> Auto-generated by `scripts/gen-docs` from `tests/poc/_descriptions.json` + rule metadata. **Jangan edit di sini** — edit JSON-nya lalu jalankan `scripts/gen-docs`.

**Rule:** `rules/plaintext/sqli.rules` · **Confidence:** high · **OWASP:** A03-Injection · **Severity:** Critical · **Network:** plain

## Serangan apa ini?
SQL injection eskalasi ke eksekusi perintah OS MSSQL via `xp_cmdshell`.

## Kalau kena, dampaknya?
Remote Code Execution di server database (kendali penuh).

## Apa yang rule ini deteksi?
`xp_cmdshell` sebagai invokasi (didahului EXEC/master../; atau diikuti (/quote) — high conf, tanpa threshold.

## PoC vs Negative
- `poc.sh` → **harus** memicu SID 9100005.
- `negative.sh` → lookalike jinak; kena fast_pattern lalu **ditolak pcre** → TIDAK alert (§19 T6).

## Cara jalanin
```
scripts/rulectl check 9100005
```
