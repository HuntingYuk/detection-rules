# SID 9150002 — WEB SSRF dangerous URL scheme (gopher/dict/file/ldap/ftp) in http.uri

> Auto-generated by `scripts/gen-docs` from `tests/poc/_descriptions.json` + rule metadata. **Jangan edit di sini** — edit JSON-nya lalu jalankan `scripts/gen-docs`.

**Rule:** `rules/plaintext/ssrf.rules` · **Confidence:** high · **OWASP:** A10-SSRF · **Severity:** Critical · **Network:** plain

## Serangan apa ini?
SSRF lewat skema URL berbahaya (gopher/dict/file/ldap/ftp) di parameter.

## Kalau kena, dampaknya?
Akses layanan internal / baca file lokal / pivot ke backend.

## Apa yang rule ini deteksi?
Skema gopher/dict/ldap/ftp/file:// sebagai nilai parameter — aplikasi normal kirim http(s).

## PoC vs Negative
- `poc.sh` → **harus** memicu SID 9150002.
- `negative.sh` → lookalike jinak; kena fast_pattern lalu **ditolak pcre** → TIDAK alert (§19 T6).

## Cara jalanin
```
scripts/rulectl check 9150002
```
