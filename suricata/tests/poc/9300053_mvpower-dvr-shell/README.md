# SID 9300053 — KNOWN-CVE MVPower DVR JAWS /shell unauth RCE (CVE-2016-20016)

> Auto-generated by `scripts/gen-docs` from `tests/poc/_descriptions.json` + rule metadata. **Jangan edit di sini** — edit JSON-nya lalu jalankan `scripts/gen-docs`.

**Rule:** `rules/plaintext/known-cve.rules` · **Confidence:** high · **OWASP:** A06-Vulnerable-and-Outdated-Components · **Severity:** Critical · **Network:** plain

## Serangan apa ini?
MVPower CCTV DVR JAWS webserver unauth RCE (CVE-2016-20016): endpoint `/shell?` bener-bener langsung mengirim query string ke shell — payload `/shell?cd /tmp; wget ...; chmod 777 ...; ./m.sh`. Stupidly trivial. Masih Mirai-recruited 2024 (18 hit real-traffic).

## Kalau kena, dampaknya?
Akses shell penuh ke DVR CCTV → privacy invasion + botnet recruit.

## Apa yang rule ini deteksi?
Path `/shell?` + query string berisi shell-command pattern (cd /, wget, curl, busybox, chmod, /tmp/, /bin/sh).

## PoC vs Negative
- `poc.sh` → **harus** memicu SID 9300053.
- `negative.sh` → lookalike jinak; kena fast_pattern lalu **ditolak pcre** → TIDAK alert (§19 T6).

## Cara jalanin
```
scripts/rulectl check 9300053
```
