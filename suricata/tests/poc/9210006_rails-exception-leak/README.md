# SID 9210006 — WEB Info Leak - Rails dev exception page in HTTP response body

> Auto-generated by `scripts/gen-docs` from `tests/poc/_descriptions.json` + rule metadata. **Jangan edit di sini** — edit JSON-nya lalu jalankan `scripts/gen-docs`.

**Rule:** `rules/plaintext/info-leak.rules` · **Confidence:** high · **OWASP:** A01-Broken-Access-Control · **Severity:** Major · **Network:** plain · **Side:** response (to_client)

## Serangan apa ini?
Aplikasi Rails error di mode development → halaman exception default 'Action Controller: Exception caught' ikut terkirim, bocorkan path file (`app/controllers/...`), nama kontroller/action, dan environment Rails.

## Kalau kena, dampaknya?
Bocornya struktur internal aplikasi Ruby on Rails (versi, path, model/view/controller) — bahan recon untuk serangan lanjutan yang lebih tepat sasaran.

## Apa yang rule ini deteksi?
Judul `Action Controller: Exception caught` + frame Rails (`<h2>`/extracted source/`app/...` path/`Rails.root`/`RAILS_ENV`) di body response.

## PoC vs Negative
- `poc.sh` → **harus** memicu SID 9210006.
- `negative.sh` → lookalike jinak; kena fast_pattern lalu **ditolak pcre** → TIDAK alert (§19 T6).

## Cara jalanin
```
scripts/rulectl check 9210006
```
