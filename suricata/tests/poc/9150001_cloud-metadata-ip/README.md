# SID 9150001 — WEB SSRF to cloud metadata 169.254.169.254 in http.uri

> Auto-generated by `scripts/gen-docs` from `tests/poc/_descriptions.json` + rule metadata. **Jangan edit di sini** — edit JSON-nya lalu jalankan `scripts/gen-docs`.

**Rule:** `rules/plaintext/ssrf.rules` · **Confidence:** high · **OWASP:** A10-SSRF · **Severity:** Critical · **Network:** plain

## Serangan apa ini?
SSRF ke layanan cloud metadata via IP link-local 169.254.169.254.

## Kalau kena, dampaknya?
Pencurian kredensial IAM instance → ambil alih akun cloud.

## Apa yang rule ini deteksi?
`169.254.169.254` tepat setelah skema (://) sebagai nilai parameter.

## PoC vs Negative
- `poc.sh` → **harus** memicu SID 9150001.
- `negative.sh` → lookalike jinak; kena fast_pattern lalu **ditolak pcre** → TIDAK alert (§19 T6).

## Cara jalanin
```
scripts/rulectl check 9150001
```
