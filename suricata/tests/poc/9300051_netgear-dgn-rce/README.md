# SID 9300051 — KNOWN-CVE Netgear DGN1000/DGN2200 setup.cgi unauth RCE (CVE-2017-6334)

> Auto-generated by `scripts/gen-docs` from `tests/poc/_descriptions.json` + rule metadata. **Jangan edit di sini** — edit JSON-nya lalu jalankan `scripts/gen-docs`.

**Rule:** `rules/plaintext/known-cve.rules` · **Confidence:** high · **OWASP:** A06-Vulnerable-and-Outdated-Components · **Severity:** Critical · **Network:** plain

## Serangan apa ini?
Netgear DGN1000/DGN2200 router unauth RCE (CVE-2017-6334): endpoint `/setup.cgi` accept parameter `next_file=` (load file config) + `todo=syscmd&cmd=...` yang executed langsung di shell tanpa sanitasi. Mass-recruited oleh Mirai (34 hit real-traffic).

## Kalau kena, dampaknya?
RCE unauth di router rumah → botnet recruit, pivot ke jaringan internal pemakai.

## Apa yang rule ini deteksi?
Path `/setup.cgi?` + parameter `next_file=*.htm` + (`cmd=` atau `curpath=`) berisi shell metachar (` $ | ;).

## PoC vs Negative
- `poc.sh` → **harus** memicu SID 9300051.
- `negative.sh` → lookalike jinak; kena fast_pattern lalu **ditolak pcre** → TIDAK alert (§19 T6).

## Cara jalanin
```
scripts/rulectl check 9300051
```
