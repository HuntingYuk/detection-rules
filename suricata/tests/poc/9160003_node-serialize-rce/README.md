# SID 9160003 — WEB Insecure Deserialization - Node.js node-serialize RCE marker in body

> Auto-generated by `scripts/gen-docs` from `tests/poc/_descriptions.json` + rule metadata. **Jangan edit di sini** — edit JSON-nya lalu jalankan `scripts/gen-docs`.

**Rule:** `rules/plaintext/deserialization.rules` · **Confidence:** high · **OWASP:** A08-Software-and-Data-Integrity-Failures · **Severity:** Critical · **Network:** plain

## Serangan apa ini?
Insecure deserialization Node.js: penyerang menyalahgunakan node-serialize `unserialize()` dengan marker fungsi IIFE.

## Kalau kena, dampaknya?
Remote Code Execution di server Node.js.

## Apa yang rule ini deteksi?
Marker `_$$ND_FUNC$$_` + fungsi IIFE di body request — unik ke payload node-serialize.

## PoC vs Negative
- `poc.sh` → **harus** memicu SID 9160003.
- `negative.sh` → lookalike jinak; kena fast_pattern lalu **ditolak pcre** → TIDAK alert (§19 T6).

## Cara jalanin
```
scripts/rulectl check 9160003
```
