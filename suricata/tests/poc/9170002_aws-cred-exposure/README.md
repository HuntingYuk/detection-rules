# SID 9170002 — WEB Exposure - /.aws/credentials cloud secret probe

> Auto-generated by `scripts/gen-docs` from `tests/poc/_descriptions.json` + rule metadata. **Jangan edit di sini** — edit JSON-nya lalu jalankan `scripts/gen-docs`.

**Rule:** `rules/plaintext/access-control.rules` · **Confidence:** high · **OWASP:** A05-Security-Misconfiguration · **Severity:** Major · **Network:** plain

## Serangan apa ini?
Penyerang mencoba mengambil file kredensial AWS CLI (/.aws/credentials) yang bocor ke web root.

## Kalau kena, dampaknya?
Bocornya access key + secret AWS = penyerang bisa ambil alih akun cloud (data, billing, infrastruktur).

## Apa yang rule ini deteksi?
Request ke path /.aws/credentials — klien jinak tidak pernah memintanya.

## PoC vs Negative
- `poc.sh` → **harus** memicu SID 9170002.
- `negative.sh` → lookalike jinak; kena fast_pattern lalu **ditolak pcre** → TIDAK alert (§19 T6).

## Cara jalanin
```
scripts/rulectl check 9170002
```
