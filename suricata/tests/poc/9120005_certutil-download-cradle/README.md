# SID 9120005 — WEB CMDi Windows certutil -urlcache download cradle in http.uri

> Auto-generated by `scripts/gen-docs` from `tests/poc/_descriptions.json` + rule metadata. **Jangan edit di sini** — edit JSON-nya lalu jalankan `scripts/gen-docs`.

**Rule:** `rules/plaintext/cmdi.rules` · **Confidence:** high · **OWASP:** A03-Injection · **Severity:** Critical · **Network:** plain

## Serangan apa ini?
Pasca command injection di Windows, penyerang pakai LOLBin `certutil -urlcache -f http://...` untuk mengunduh malware tahap-2.

## Kalau kena, dampaknya?
Penarikan & eksekusi malware (beacon C2, ransomware) — pijakan jadi kendali penuh persisten.

## Apa yang rule ini deteksi?
Kombinasi `certutil` + `-urlcache` + URL http(s) dalam jendela terbatas di URL request.

## PoC vs Negative
- `poc.sh` → **harus** memicu SID 9120005.
- `negative.sh` → lookalike jinak; kena fast_pattern lalu **ditolak pcre** → TIDAK alert (§19 T6).

## Cara jalanin
```
scripts/rulectl check 9120005
```
