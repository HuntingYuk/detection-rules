# SID 9120001 — WEB CMDi shell-separator + cat /etc/ in http.uri

> Auto-generated by `scripts/gen-docs` from `tests/poc/_descriptions.json` + rule metadata. **Jangan edit di sini** — edit JSON-nya lalu jalankan `scripts/gen-docs`.

**Rule:** `rules/plaintext/cmdi.rules` · **Confidence:** medium · **OWASP:** A03-Injection · **Severity:** Critical · **Network:** plain

## Serangan apa ini?
Command injection: penyerang menyisipkan pemisah shell (; | & atau backtick) lalu perintah `cat /etc/...` di parameter URL untuk membaca file sensitif server.

## Kalau kena, dampaknya?
Pembacaan file rahasia server (mis. /etc/passwd, config) — pintu masuk ke kompromi penuh.

## Apa yang rule ini deteksi?
Pola pemisah-shell tepat sebelum `cat /etc/` di URL; URL sah tidak pernah membawanya.

## PoC vs Negative
- `poc.sh` → **harus** memicu SID 9120001.
- `negative.sh` → lookalike jinak; kena fast_pattern lalu **ditolak pcre** → TIDAK alert (§19 T6).

## Cara jalanin
```
scripts/rulectl check 9120001
```
