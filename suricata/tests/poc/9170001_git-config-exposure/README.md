# SID 9170001 — WEB Exposure - /.git/config repository probe

> Auto-generated by `scripts/gen-docs` from `tests/poc/_descriptions.json` + rule metadata. **Jangan edit di sini** — edit JSON-nya lalu jalankan `scripts/gen-docs`.

**Rule:** `rules/plaintext/access-control.rules` · **Confidence:** high · **OWASP:** A05-Security-Misconfiguration · **Severity:** Major · **Network:** plain

## Serangan apa ini?
Penyerang menebak path file internal Git yang nggak sengaja ke-publish ke web root, yaitu /.git/config.

## Kalau kena, dampaknya?
Kalau folder .git ke-expose, seluruh source code + history + secret yang pernah ke-commit bisa diunduh.

## Apa yang rule ini deteksi?
Request ke path /.git/config — tidak ada klien sah yang minta file ini.

## PoC vs Negative
- `poc.sh` → **harus** memicu SID 9170001.
- `negative.sh` → lookalike jinak; kena fast_pattern lalu **ditolak pcre** → TIDAK alert (§19 T6).

## Cara jalanin
```
scripts/rulectl check 9170001
```
