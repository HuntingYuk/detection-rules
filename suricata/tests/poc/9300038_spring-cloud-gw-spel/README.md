# SID 9300038 — KNOWN-CVE Spring Cloud Gateway SpEL RCE via actuator route (CVE-2022-22947)

> Auto-generated by `scripts/gen-docs` from `tests/poc/_descriptions.json` + rule metadata. **Jangan edit di sini** — edit JSON-nya lalu jalankan `scripts/gen-docs`.

**Rule:** `rules/plaintext/known-cve.rules` · **Confidence:** high · **OWASP:** A06-Vulnerable-and-Outdated-Components · **Severity:** Critical · **Network:** plain

## Serangan apa ini?
Spring Cloud Gateway SpEL RCE (CVE-2022-22947): endpoint Actuator `/actuator/gateway/routes/{name}` menerima definisi route berisi ekspresi SpEL `#{T(java.lang.Runtime)...}` yang dieksekusi saat refresh.

## Kalau kena, dampaknya?
Remote Code Execution di service gateway (dampak luas karena gateway berada di edge).

## Apa yang rule ini deteksi?
POST ke `/actuator/gateway/routes/...` + body berisi SpEL gadget (`T(java.lang.Runtime|ProcessBuilder)` / `getRuntime().exec`).

## PoC vs Negative
- `poc.sh` → **harus** memicu SID 9300038.
- `negative.sh` → lookalike jinak; kena fast_pattern lalu **ditolak pcre** → TIDAK alert (§19 T6).

## Cara jalanin
```
scripts/rulectl check 9300038
```
