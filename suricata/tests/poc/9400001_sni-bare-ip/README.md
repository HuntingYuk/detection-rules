# SID 9400001 — TLS anomaly - SNI is a bare IPv4 literal (non-RFC6066, suspicious client)

> Auto-generated by `scripts/gen-docs` from `tests/poc/_descriptions.json` + rule metadata. **Jangan edit di sini** — edit JSON-nya lalu jalankan `scripts/gen-docs`.

**Rule:** `rules/tls-meta/tls-anomaly.rules` · **Confidence:** medium · **OWASP:** none · **Severity:** Major · **Network:** tls

## Serangan apa ini?
TLS anomaly: SNI berupa IPv4 literal (melanggar RFC 6066) — klien mencurigakan tanpa perlu dekripsi.

## Kalau kena, dampaknya?
Indikator tooling/malware (klien sah selalu kirim hostname).

## Apa yang rule ini deteksi?
SNI di ClientHello = IPv4 literal (cleartext, tanpa dekripsi).

## PoC vs Negative
- `poc.sh` → **harus** memicu SID 9400001.
- `negative.sh` → lookalike jinak; kena fast_pattern lalu **ditolak pcre** → TIDAK alert (§19 T6).

## Cara jalanin
```
scripts/rulectl check 9400001
```
