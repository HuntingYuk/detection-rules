# SID 9170008 — WEB Exposure - dependency-manifest probe (composer/package/yarn/bower/Gemfile/pom/gradle)

> Auto-generated by `scripts/gen-docs` from `tests/poc/_descriptions.json` + rule metadata. **Jangan edit di sini** — edit JSON-nya lalu jalankan `scripts/gen-docs`.

**Rule:** `rules/plaintext/access-control.rules` · **Confidence:** high · **OWASP:** A05-Security-Misconfiguration · **Severity:** Major · **Network:** plain

## Serangan apa ini?
Penyerang nge-probe file dependency-manifest yang nggak sengaja ke-publish ke web root: `composer.json` (PHP), `package.json`/`package-lock.json` (Node.js), `yarn.lock`, `bower.json`, `Gemfile`/`Gemfile.lock` (Ruby), `pom.xml`/`build.gradle` (Java). Standar scanner recon.

## Kalau kena, dampaknya?
Bocornya daftar dependency + versi → bahan recon supply-chain attack (cari versi vulnerable yang ke-load app korban) + kadang ke-leak script command + author info di dalam.

## Apa yang rule ini deteksi?
Request ke salah satu canonical manifest path (`/composer.{json,lock}`, `/package(-lock)?.json`, `/yarn.lock`, `/bower.json`, `/Gemfile(.lock)?`, `/pom.xml`, `/build.gradle`).

## PoC vs Negative
- `poc.sh` → **harus** memicu SID 9170008.
- `negative.sh` → lookalike jinak; kena fast_pattern lalu **ditolak pcre** → TIDAK alert (§19 T6).

## Cara jalanin
```
scripts/rulectl check 9170008
```
