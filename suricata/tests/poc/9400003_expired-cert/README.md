# SID 9400003 — TLS anomaly - expired server certificate presented

> Auto-generated by `scripts/gen-docs` from `tests/poc/_descriptions.json` + rule metadata. **Jangan edit di sini** — edit JSON-nya lalu jalankan `scripts/gen-docs`.

**Rule:** `rules/tls-meta/tls-anomaly.rules` · **Confidence:** medium · **OWASP:** none · **Severity:** Major · **Network:** tls

## Serangan apa ini?
TLS anomaly: sertifikat server yang dipresentasikan sudah kedaluwarsa.

## Kalau kena, dampaknya?
Indikator infrastruktur terbengkalai / MITM / phishing.

## Apa yang rule ini deteksi?
Sertifikat server melewati notAfter (tls_cert_expired, dievaluasi engine; cleartext di handshake).

## PoC vs Negative
- `poc.sh` → **harus** memicu SID 9400003.
- `negative.sh` → lookalike jinak; kena fast_pattern lalu **ditolak pcre** → TIDAK alert (§19 T6).

## Cara jalanin
```
scripts/rulectl check 9400003
```
