# SID 9140003 — WEB XXE/DoS recursive entity expansion (billion laughs) in request body

> Auto-generated by `scripts/gen-docs` from `tests/poc/_descriptions.json` + rule metadata. **Jangan edit di sini** — edit JSON-nya lalu jalankan `scripts/gen-docs`.

**Rule:** `rules/plaintext/xxe.rules` · **Confidence:** high · **OWASP:** A03-Injection · **Severity:** Major · **Network:** plain

## Serangan apa ini?
XML entity-expansion DoS ('billion laughs'): entity rekursif yang meledak secara eksponensial.

## Kalau kena, dampaknya?
Denial of Service — habiskan memori/CPU server XML.

## Apa yang rule ini deteksi?
`<!ENTITY` yang didefinisikan sebagai >=2 referensi ke entity lain (amplifier rekursif).

## PoC vs Negative
- `poc.sh` → **harus** memicu SID 9140003.
- `negative.sh` → lookalike jinak; kena fast_pattern lalu **ditolak pcre** → TIDAK alert (§19 T6).

## Cara jalanin
```
scripts/rulectl check 9140003
```
