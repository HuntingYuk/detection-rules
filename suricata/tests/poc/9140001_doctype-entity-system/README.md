# SID 9140001 — WEB XXE external entity (SYSTEM/PUBLIC) in request body

> Auto-generated by `scripts/gen-docs` from `tests/poc/_descriptions.json` + rule metadata. **Jangan edit di sini** — edit JSON-nya lalu jalankan `scripts/gen-docs`.

**Rule:** `rules/plaintext/xxe.rules` · **Confidence:** high · **OWASP:** A03-Injection · **Severity:** Critical · **Network:** plain

## Serangan apa ini?
XML External Entity (XXE): entity eksternal SYSTEM/PUBLIC di body XML untuk membaca file/SSRF.

## Kalau kena, dampaknya?
Pembacaan file server, SSRF, kadang RCE.

## Apa yang rule ini deteksi?
`<!ENTITY` + entity EKSTERNAL (SYSTEM/PUBLIC + skema fetchable) di body request.

## PoC vs Negative
- `poc.sh` → **harus** memicu SID 9140001.
- `negative.sh` → lookalike jinak; kena fast_pattern lalu **ditolak pcre** → TIDAK alert (§19 T6).

## Cara jalanin
```
scripts/rulectl check 9140001
```
