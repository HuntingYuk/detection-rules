# SID 9120009 — WEB CMDi python -c inline-script execution in http.uri

> Auto-generated by `scripts/gen-docs` from `tests/poc/_descriptions.json` + rule metadata. **Jangan edit di sini** — edit JSON-nya lalu jalankan `scripts/gen-docs`.

**Rule:** `rules/plaintext/cmdi.rules` · **Confidence:** high · **OWASP:** A03-Injection · **Severity:** Critical · **Network:** plain

## Serangan apa ini?
Pasca command injection, penyerang pakai `python -c '<script>'` untuk eksekusi inline berisi `os.system`/`subprocess`/`socket.socket` (reverse shell, exec perintah arbitrer).

## Kalau kena, dampaknya?
Remote Code Execution / reverse shell di server target — penyerang dapat shell penuh.

## Apa yang rule ini deteksi?
`python -c` + script berisi import berbahaya (`os.system`/`subprocess.Popen|run|call`/`socket.socket`/`__import__`) di URL.

## PoC vs Negative
- `poc.sh` → **harus** memicu SID 9120009.
- `negative.sh` → lookalike jinak; kena fast_pattern lalu **ditolak pcre** → TIDAK alert (§19 T6).

## Cara jalanin
```
scripts/rulectl check 9120009
```
