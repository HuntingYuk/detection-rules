# SID 9100002 — WEB SQLi time-based blind (SLEEP/BENCHMARK/WAITFOR) in http.uri

> Auto-generated by `scripts/gen-docs` from `tests/poc/_descriptions.json` + rule metadata. **Jangan edit di sini** — edit JSON-nya lalu jalankan `scripts/gen-docs`.

**Rule:** `rules/plaintext/sqli.rules` · **Confidence:** medium · **OWASP:** A03-Injection · **Severity:** Major · **Network:** plain

## Serangan apa ini?
SQL injection blind time-based: penyerang memakai SLEEP/BENCHMARK/WAITFOR untuk menyimpulkan data dari delay respons.

## Kalau kena, dampaknya?
Eksfiltrasi data perlahan walau hasil query tak terlihat.

## Apa yang rule ini deteksi?
Primitif waktu (SLEEP()/PG_SLEEP()/BENCHMARK()/WAITFOR DELAY) di URL; threshold 3/60 detik.

## PoC vs Negative
- `poc.sh` → **harus** memicu SID 9100002.
- `negative.sh` → lookalike jinak; kena fast_pattern lalu **ditolak pcre** → TIDAK alert (§19 T6).

## Cara jalanin
```
scripts/rulectl check 9100002
```
