# SID 9500002 — PROTO-ANOMALY TCP Xmas scan (FIN+PSH+URG)

> Auto-generated by `scripts/gen-docs` from `tests/poc/_descriptions.json` + rule metadata. **Jangan edit di sini** — edit JSON-nya lalu jalankan `scripts/gen-docs`.

**Rule:** `rules/tls-meta/protocol-anomaly.rules` · **Confidence:** high · **OWASP:** none · **Severity:** Minor · **Network:** any

## Serangan apa ini?
Protocol anomaly: TCP Xmas scan (FIN+PSH+URG).

## Kalau kena, dampaknya?
Indikator pemindaian stealth.

## Apa yang rule ini deteksi?
Kombinasi flag FIN+PSH+URG (single-event).

## PoC vs Negative
- `poc.sh` → **harus** memicu SID 9500002.
- `negative.sh` → lookalike jinak; kena fast_pattern lalu **ditolak pcre** → TIDAK alert (§19 T6).

## Cara jalanin
```
scripts/rulectl check 9500002
```
