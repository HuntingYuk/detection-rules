# SID 9200001 — WEB Recon - sqlmap scanner User-Agent

> Auto-generated by `scripts/gen-docs` from `tests/poc/_descriptions.json` + rule metadata. **Jangan edit di sini** — edit JSON-nya lalu jalankan `scripts/gen-docs`.

**Rule:** `rules/plaintext/recon-http.rules` · **Confidence:** high · **OWASP:** none · **Severity:** Major · **Network:** plain

## Serangan apa ini?
Recon: pemindaian otomatis oleh sqlmap (tool SQLi) yang memakai User-Agent default-nya.

## Kalau kena, dampaknya?
Indikator awal serangan SQLi terotomasi terhadap aplikasi.

## Apa yang rule ini deteksi?
User-Agent `sqlmap` — tidak ada klien sah yang mengiklankannya.

## PoC vs Negative
- `poc.sh` → **harus** memicu SID 9200001.
- `negative.sh` → lookalike jinak; kena fast_pattern lalu **ditolak pcre** → TIDAK alert (§19 T6).

## Cara jalanin
```
scripts/rulectl check 9200001
```
