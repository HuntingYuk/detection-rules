# SID 9120003 — WEB SSTI computed probe {{7*7}} (Jinja/Twig) in http.uri

> Auto-generated by `scripts/gen-docs` from `tests/poc/_descriptions.json` + rule metadata. **Jangan edit di sini** — edit JSON-nya lalu jalankan `scripts/gen-docs`.

**Rule:** `rules/plaintext/cmdi.rules` · **Confidence:** medium · **OWASP:** A03-Injection · **Severity:** Major · **Network:** plain

## Serangan apa ini?
Server-Side Template Injection (SSTI): penyerang mengirim probe aritmetika `{{7*7}}` (Jinja/Twig) untuk menguji apakah input dieksekusi sebagai template.

## Kalau kena, dampaknya?
SSTI yang berhasil biasanya berujung Remote Code Execution di server.

## Apa yang rule ini deteksi?
Literal probe `{{7*7}}` di URL (threshold 3/60 detik sebagai kompensator FP).

## PoC vs Negative
- `poc.sh` → **harus** memicu SID 9120003.
- `negative.sh` → lookalike jinak; kena fast_pattern lalu **ditolak pcre** → TIDAK alert (§19 T6).

## Cara jalanin
```
scripts/rulectl check 9120003
```
