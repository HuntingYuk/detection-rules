# SID 9210007 — WEB Info Leak - Apache Tomcat default error page in HTTP response body

> Auto-generated by `scripts/gen-docs` from `tests/poc/_descriptions.json` + rule metadata. **Jangan edit di sini** — edit JSON-nya lalu jalankan `scripts/gen-docs`.

**Rule:** `rules/plaintext/info-leak.rules` · **Confidence:** high · **OWASP:** A01-Broken-Access-Control · **Severity:** Major · **Network:** plain · **Side:** response (to_client)

## Serangan apa ini?
Aplikasi Java/Tomcat error tapi halaman error default 'Apache Tomcat/X.Y.Z - Error report' ikut terkirim, bocorkan versi Tomcat + stack servlet + path aplikasi.

## Kalau kena, dampaknya?
Bocornya versi & struktur server Java (Tomcat version → exploit version-spesifik) — bahan recon untuk serangan lanjutan yang lebih tepat sasaran.

## Apa yang rule ini deteksi?
Header `Apache Tomcat/X.Y.Z` + struktur error-report (`HTTP Status N`, Exception/Status Report, Description, root cause) di body response.

## PoC vs Negative
- `poc.sh` → **harus** memicu SID 9210007.
- `negative.sh` → lookalike jinak; kena fast_pattern lalu **ditolak pcre** → TIDAK alert (§19 T6).

## Cara jalanin
```
scripts/rulectl check 9210007
```
