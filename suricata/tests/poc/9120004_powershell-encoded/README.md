# SID 9120004 — WEB CMDi PowerShell encoded command in http.uri

> Auto-generated by `scripts/gen-docs` from `tests/poc/_descriptions.json` + rule metadata. **Jangan edit di sini** — edit JSON-nya lalu jalankan `scripts/gen-docs`.

**Rule:** `rules/plaintext/cmdi.rules` · **Confidence:** medium · **OWASP:** A03-Injection · **Severity:** Critical · **Network:** plain

## Serangan apa ini?
Command injection PowerShell: penyerang menjalankan perintah ter-encode base64 lewat flag `-EncodedCommand` agar lolos filter.

## Kalau kena, dampaknya?
Eksekusi perintah arbitrer di server Windows (RCE).

## Apa yang rule ini deteksi?
`powershell` + flag -e/-enc/-EncodedCommand diikuti blob base64 panjang (threshold 3/60 detik).

## PoC vs Negative
- `poc.sh` → **harus** memicu SID 9120004.
- `negative.sh` → lookalike jinak; kena fast_pattern lalu **ditolak pcre** → TIDAK alert (§19 T6).

## Cara jalanin
```
scripts/rulectl check 9120004
```
