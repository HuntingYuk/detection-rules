# SID 9300048 — KNOWN-CVE ThinkPHP invokefunction route RCE (CVE-2018-20062)

> Auto-generated by `scripts/gen-docs` from `tests/poc/_descriptions.json` + rule metadata. **Jangan edit di sini** — edit JSON-nya lalu jalankan `scripts/gen-docs`.

**Rule:** `rules/plaintext/known-cve.rules` · **Confidence:** high · **OWASP:** A06-Vulnerable-and-Outdated-Components · **Severity:** Critical · **Network:** plain

## Serangan apa ini?
ThinkPHP 5.x invokefunction-route RCE (CVE-2018-20062 family): bug route ThinkPHP yang nge-resolve `_method`/`s=` jadi pemanggilan callable PHP arbitrer — payload klasik `?s=/Index/\think\app/invokefunction&function=call_user_func_array&vars[0]=system&vars[1][]=cmd`. Ke-scan massal botnet (86 hit di 3 pcap MTA scan-and-probe).

## Kalau kena, dampaknya?
Remote Code Execution pre-auth di aplikasi PHP yang pakai framework ThinkPHP — sering di-pwn jadi server proxy / miner / web-shell.

## Apa yang rule ini deteksi?
Path URL berisi `invokefunction` + `function=` + gadget eksekusi PHP (system/exec/passthru/shell_exec/phpinfo/assert/eval/file_get_contents/call_user_func).

## PoC vs Negative
- `poc.sh` → **harus** memicu SID 9300048.
- `negative.sh` → lookalike jinak; kena fast_pattern lalu **ditolak pcre** → TIDAK alert (§19 T6).

## Cara jalanin
```
scripts/rulectl check 9300048
```
