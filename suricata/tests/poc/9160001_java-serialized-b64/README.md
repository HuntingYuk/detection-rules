# SID 9160001 — WEB Insecure Deserialization - Java serialized object (base64 rO0AB) in request body

> Auto-generated by `scripts/gen-docs` from `tests/poc/_descriptions.json` + rule metadata. **Jangan edit di sini** — edit JSON-nya lalu jalankan `scripts/gen-docs`.

**Rule:** `rules/plaintext/deserialization.rules` · **Confidence:** high · **OWASP:** A08-Software-and-Data-Integrity-Failures · **Severity:** Critical · **Network:** plain

## Serangan apa ini?
Insecure deserialization Java: penyerang mengirim objek Java ter-serialisasi (ysoserial gadget) yang langsung mengeksekusi perintah saat di-deserialize.

## Kalau kena, dampaknya?
Remote Code Execution penuh di server aplikasi Java.

## Apa yang rule ini deteksi?
Magic base64 `rO0AB` (header serialisasi Java AC ED 00 05) di body request — tidak pernah ada di data jinak.

## PoC vs Negative
- `poc.sh` → **harus** memicu SID 9160001.
- `negative.sh` → lookalike jinak; kena fast_pattern lalu **ditolak pcre** → TIDAK alert (§19 T6).

## Cara jalanin
```
scripts/rulectl check 9160001
```
