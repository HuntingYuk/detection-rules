# SID 9210005 — WEB Info Leak - Java Spring Boot Whitelabel Error Page in HTTP response body

> Auto-generated by `scripts/gen-docs` from `tests/poc/_descriptions.json` + rule metadata. **Jangan edit di sini** — edit JSON-nya lalu jalankan `scripts/gen-docs`.

**Rule:** `rules/plaintext/info-leak.rules` · **Confidence:** high · **OWASP:** A01-Broken-Access-Control · **Severity:** Major · **Network:** plain · **Side:** response (to_client)

## Serangan apa ini?
Aplikasi Spring Boot error tapi konfigurasi salah → halaman debug default 'Whitelabel Error Page' ikut terkirim ke browser, bocorkan versi Spring + path + detail exception.

## Kalau kena, dampaknya?
Bocornya struktur internal aplikasi Java (versi framework, path endpoint, exception) — bahan recon untuk serangan lanjutan yang lebih tepat sasaran.

## Apa yang rule ini deteksi?
`Whitelabel Error Page` + struktur error (status=/timestamp/Exception/'There was an unexpected error') di body response.

## PoC vs Negative
- `poc.sh` → **harus** memicu SID 9210005.
- `negative.sh` → lookalike jinak; kena fast_pattern lalu **ditolak pcre** → TIDAK alert (§19 T6).

## Cara jalanin
```
scripts/rulectl check 9210005
```
