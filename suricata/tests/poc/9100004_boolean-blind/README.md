# SID 9100004 — WEB SQLi boolean-blind tautology in http.uri

> Auto-generated by `scripts/gen-docs` from `tests/poc/_descriptions.json` + rule metadata. **Jangan edit di sini** — edit JSON-nya lalu jalankan `scripts/gen-docs`.

**Rule:** `rules/plaintext/sqli.rules` · **Confidence:** medium · **OWASP:** A03-Injection · **Severity:** Major · **Network:** plain

## Serangan apa ini?
SQL injection blind boolean: tautologi `OR 1=1` dengan batas quote/paren untuk menyimpulkan data dari beda respons.

## Kalau kena, dampaknya?
Eksfiltrasi data / bypass kondisi (mis. login).

## Apa yang rule ini deteksi?
Tautologi SQL dengan batas quote/paren ('or'1'='1 / )or(1=1 / or 1=1--); threshold 3/60 detik.

## PoC vs Negative
- `poc.sh` → **harus** memicu SID 9100004.
- `negative.sh` → lookalike jinak; kena fast_pattern lalu **ditolak pcre** → TIDAK alert (§19 T6).

## Cara jalanin
```
scripts/rulectl check 9100004
```
