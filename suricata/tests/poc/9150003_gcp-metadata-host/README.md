# SID 9150003 — WEB SSRF to GCP cloud metadata hostname metadata.google.internal in http.uri

> Auto-generated by `scripts/gen-docs` from `tests/poc/_descriptions.json` + rule metadata. **Jangan edit di sini** — edit JSON-nya lalu jalankan `scripts/gen-docs`.

**Rule:** `rules/plaintext/ssrf.rules` · **Confidence:** high · **OWASP:** A10-SSRF · **Severity:** Critical · **Network:** plain

## Serangan apa ini?
SSRF ke layanan metadata GCP via hostname `metadata.google.internal` (bukan IP).

## Kalau kena, dampaknya?
Pencurian token service-account GCP → ambil alih proyek cloud.

## Apa yang rule ini deteksi?
`metadata.google.internal` tepat setelah skema atau '=' (sebagai target, bukan teks dokumentasi).

## PoC vs Negative
- `poc.sh` → **harus** memicu SID 9150003.
- `negative.sh` → lookalike jinak; kena fast_pattern lalu **ditolak pcre** → TIDAK alert (§19 T6).

## Cara jalanin
```
scripts/rulectl check 9150003
```
