# SID 9130006 — WEB Path Traversal nested-dot bypass ....// in http.uri.raw

> Auto-generated by `scripts/gen-docs` from `tests/poc/_descriptions.json` + rule metadata. **Jangan edit di sini** — edit JSON-nya lalu jalankan `scripts/gen-docs`.

**Rule:** `rules/plaintext/traversal.rules` · **Confidence:** high · **OWASP:** A03-Injection · **Severity:** Critical · **Network:** plain

## Serangan apa ini?
Path traversal evasion: pola `....//` — setelah filter single-pass mencabut `..`, sisa karakter jadi `../`. WAF yang blokir `../` literal akan lewat.

## Kalau kena, dampaknya?
Pembacaan file sistem sensitif (LFI) lewat bypass filter sederhana — pijakan recon/kompromi.

## Apa yang rule ini deteksi?
Literal `....//` di URL RAW + konteks sensitif setelahnya (`etc/passwd`, `windows`, `win.ini`, `/proc/self`).

## PoC vs Negative
- `poc.sh` → **harus** memicu SID 9130006.
- `negative.sh` → lookalike jinak; kena fast_pattern lalu **ditolak pcre** → TIDAK alert (§19 T6).

## Cara jalanin
```
scripts/rulectl check 9130006
```
