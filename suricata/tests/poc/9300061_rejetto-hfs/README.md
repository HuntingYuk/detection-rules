# SID 9300061 — KNOWN-CVE Rejetto HFS template-exec RCE (CVE-2024-23692)

> Auto-generated by `scripts/gen-docs` from `tests/poc/_descriptions.json` + rule metadata. **Jangan edit di sini** — edit JSON-nya lalu jalankan `scripts/gen-docs`.

**Rule:** `rules/plaintext/known-cve.rules` · **Confidence:** high · **OWASP:** A06-Vulnerable-and-Outdated-Components · **Severity:** Critical · **Network:** plain

## Serangan apa ini?
Rejetto HTTP File Server (HFS) 2.x unauth RCE (CVE-2024-23692): template engine bawaan nge-evaluate token `{.exec|<cmd>.|}` di parameter `search=` (dan lain). Penyerang kirim `?search=%00{.exec|cmd.|}` → cmd dijalankan di server.

## Kalau kena, dampaknya?
Remote Code Execution tanpa auth di HFS — file-server populer di kalangan small business / power user, sering ke-expose ke internet.

## Apa yang rule ini deteksi?
Token template HFS `{.exec|...|}` (atau set/file/comment variant) di URL request.

## PoC vs Negative
- `poc.sh` → **harus** memicu SID 9300061.
- `negative.sh` → lookalike jinak; kena fast_pattern lalu **ditolak pcre** → TIDAK alert (§19 T6).

## Cara jalanin
```
scripts/rulectl check 9300061
```
