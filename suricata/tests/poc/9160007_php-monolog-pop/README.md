# SID 9160007 — WEB Insecure Deserialization - PHP object injection Monolog POP chain (O:NN:\

> Auto-generated by `scripts/gen-docs` from `tests/poc/_descriptions.json` + rule metadata. **Jangan edit di sini** — edit JSON-nya lalu jalankan `scripts/gen-docs`.

**Rule:** `rules/plaintext/deserialization.rules` · **Confidence:** high · **OWASP:** A08-Software-and-Data-Integrity-Failures · **Severity:** Critical · **Network:** plain

## Serangan apa ini?
Insecure deserialization PHP: object injection memakai class gadget Monolog (POP-chain Laravel/Symfony) — objek ter-serialisasi `O:NN:"Monolog\Handler\..."` di-deserialize jadi panggilan berbahaya.

## Kalau kena, dampaknya?
Remote Code Execution di server PHP yang men-`unserialize()` data tidak terpercaya.

## Apa yang rule ini deteksi?
Marker `Monolog\Handler` dalam struktur PHP serialized-object `O:NN:"...":` di body request.

## PoC vs Negative
- `poc.sh` → **harus** memicu SID 9160007.
- `negative.sh` → lookalike jinak; kena fast_pattern lalu **ditolak pcre** → TIDAK alert (§19 T6).

## Cara jalanin
```
scripts/rulectl check 9160007
```
