# SID 9210002 — WEB Info Leak - PEM private key in HTTP response body

> Auto-generated by `scripts/gen-docs` from `tests/poc/_descriptions.json` + rule metadata. **Jangan edit di sini** — edit JSON-nya lalu jalankan `scripts/gen-docs`.

**Rule:** `rules/plaintext/info-leak.rules` · **Confidence:** high · **OWASP:** A01-Broken-Access-Control · **Severity:** Critical · **Network:** plain · **Side:** response (to_client)

## Serangan apa ini?
Server keliru membocorkan private key (PEM) di body response.

## Kalau kena, dampaknya?
Private key bocor = penyamaran server/TLS, dekripsi, akses SSH — kompromi kepercayaan.

## Apa yang rule ini deteksi?
Header PEM `-----BEGIN ... PRIVATE KEY-----` di body response.

## PoC vs Negative
- `poc.sh` → **harus** memicu SID 9210002.
- `negative.sh` → lookalike jinak; kena fast_pattern lalu **ditolak pcre** → TIDAK alert (§19 T6).

## Cara jalanin
```
scripts/rulectl check 9210002
```
