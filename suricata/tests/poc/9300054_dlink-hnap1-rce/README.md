# SID 9300054 — KNOWN-CVE D-Link HNAP1 unauth RCE (CVE-2019-16920)

> Auto-generated by `scripts/gen-docs` from `tests/poc/_descriptions.json` + rule metadata. **Jangan edit di sini** — edit JSON-nya lalu jalankan `scripts/gen-docs`.

**Rule:** `rules/plaintext/known-cve.rules` · **Confidence:** high · **OWASP:** A06-Vulnerable-and-Outdated-Components · **Severity:** Critical · **Network:** plain

## Serangan apa ini?
D-Link DIR-859/825 router unauth RCE (CVE-2019-16920): endpoint SOAP-RPC `/HNAP1/` accept body XML; field `service` di-pass ke shell tanpa sanitasi. Variant exploit umum 'GetDeviceSettings' + metachar di service.

## Kalau kena, dampaknya?
RCE unauth di router D-Link → botnet recruit, pivot LAN.

## Apa yang rule ini deteksi?
POST ke `/HNAP1/` + body XML element `<service>` berisi shell metachar (` $ | ;) — dua buffer.

## PoC vs Negative
- `poc.sh` → **harus** memicu SID 9300054.
- `negative.sh` → lookalike jinak; kena fast_pattern lalu **ditolak pcre** → TIDAK alert (§19 T6).

## Cara jalanin
```
scripts/rulectl check 9300054
```
