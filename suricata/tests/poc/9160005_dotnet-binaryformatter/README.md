# SID 9160005 — WEB Insecure Deserialization - .NET BinaryFormatter serialized stream (base64 AAEAAAD) in request body

> Auto-generated by `scripts/gen-docs` from `tests/poc/_descriptions.json` + rule metadata. **Jangan edit di sini** — edit JSON-nya lalu jalankan `scripts/gen-docs`.

**Rule:** `rules/plaintext/deserialization.rules` · **Confidence:** high · **OWASP:** A08-Software-and-Data-Integrity-Failures · **Severity:** Critical · **Network:** plain

## Serangan apa ini?
Insecure deserialization .NET: stream biner BinaryFormatter/LosFormatter mentah (ysoserial.net) di-base64, mis. lewat __VIEWSTATE.

## Kalau kena, dampaknya?
Remote Code Execution penuh di server .NET — salah satu bug terparah di ekosistem .NET.

## Apa yang rule ini deteksi?
Magic base64 `AAEAAAD/////` (header BinaryFormatter) diikuti stream base64 di body request.

## PoC vs Negative
- `poc.sh` → **harus** memicu SID 9160005.
- `negative.sh` → lookalike jinak; kena fast_pattern lalu **ditolak pcre** → TIDAK alert (§19 T6).

## Cara jalanin
```
scripts/rulectl check 9160005
```
