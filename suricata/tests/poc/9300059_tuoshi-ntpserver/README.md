# SID 9300059 — KNOWN-CVE Tuoshi/Belkin/DigiEver ntpserver parameter OS command injection (CVE-2025-43989)

> Auto-generated by `scripts/gen-docs` from `tests/poc/_descriptions.json` + rule metadata. **Jangan edit di sini** — edit JSON-nya lalu jalankan `scripts/gen-docs`.

**Rule:** `rules/plaintext/known-cve.rules` · **Confidence:** high · **OWASP:** A06-Vulnerable-and-Outdated-Components · **Severity:** Critical · **Network:** plain

## Serangan apa ini?
Tuoshi/Belkin/DigiEver/Edimax NTP-server-config command injection (CVE-2025-43989 family): handler CGI NTP-config accept parameter `ntpserver=` di-pass langsung ke shell. Shape dipakai banyak OEM small-router. 10 hit real-traffic (Tuoshi alone, family-shape lebih besar).

## Kalau kena, dampaknya?
RCE unauth di puluhan brand router kecil → botnet recruit + pivot LAN.

## Apa yang rule ini deteksi?
Path CGI (`*.cgi`/`*.asp`) + parameter `ntpserver=` berisi shell metachar (` $ | ;).

## PoC vs Negative
- `poc.sh` → **harus** memicu SID 9300059.
- `negative.sh` → lookalike jinak; kena fast_pattern lalu **ditolak pcre** → TIDAK alert (§19 T6).

## Cara jalanin
```
scripts/rulectl check 9300059
```
