# SID 9300045 — KNOWN-CVE WordPress Bricks Builder render_element RCE (CVE-2024-25600)

> Auto-generated by `scripts/gen-docs` from `tests/poc/_descriptions.json` + rule metadata. **Jangan edit di sini** — edit JSON-nya lalu jalankan `scripts/gen-docs`.

**Rule:** `rules/plaintext/known-cve.rules` · **Confidence:** high · **OWASP:** A06-Vulnerable-and-Outdated-Components · **Severity:** Critical · **Network:** plain

## Serangan apa ini?
WordPress Bricks Builder pre-auth RCE (CVE-2024-25600): endpoint REST `/wp-json/bricks/v1/render_element` meng-evaluasi expression PHP di field `queryEditor` dari body, sehingga penyerang menyuntik gadget `eval/system/passthru/...`.

## Kalau kena, dampaknya?
Remote Code Execution tanpa auth di situs WordPress yang pakai Bricks Builder — situs di-takeover dalam satu request.

## Apa yang rule ini deteksi?
POST ke `/wp-json/bricks/v1/render_element` + body memuat `queryEditor` + gadget eksekusi PHP (eval/file_get_contents/phpinfo/passthru/system/shell_exec/assert) — dua buffer.

## PoC vs Negative
- `poc.sh` → **harus** memicu SID 9300045.
- `negative.sh` → lookalike jinak; kena fast_pattern lalu **ditolak pcre** → TIDAK alert (§19 T6).

## Cara jalanin
```
scripts/rulectl check 9300045
```
