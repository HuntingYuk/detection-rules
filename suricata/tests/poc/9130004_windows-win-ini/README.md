# SID 9130004 — WEB Path Traversal to Windows win.ini in http.uri.raw

> Auto-generated by `scripts/gen-docs` from `tests/poc/_descriptions.json` + rule metadata. **Jangan edit di sini** — edit JSON-nya lalu jalankan `scripts/gen-docs`.

**Rule:** `rules/plaintext/traversal.rules` · **Confidence:** high · **OWASP:** A03-Injection · **Severity:** Critical · **Network:** plain

## Serangan apa ini?
Path traversal menyasar Windows: keluar direktori dengan `..\` (raw/encoded) untuk membaca file `win.ini`.

## Kalau kena, dampaknya?
Pembacaan file sistem Windows (LFI) — pijakan pemetaan/kompromi (9130001 = target /etc/passwd di *nix).

## Apa yang rule ini deteksi?
`win.ini` didahului sekuens traversal (../ atau ..\ raw atau %2e%2e/%5c) di URL RAW.

## PoC vs Negative
- `poc.sh` → **harus** memicu SID 9130004.
- `negative.sh` → lookalike jinak; kena fast_pattern lalu **ditolak pcre** → TIDAK alert (§19 T6).

## Cara jalanin
```
scripts/rulectl check 9130004
```
