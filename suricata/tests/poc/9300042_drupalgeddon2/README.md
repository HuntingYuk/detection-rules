# SID 9300042 — KNOWN-CVE Drupal Drupalgeddon2 #post_render Form-API RCE (CVE-2018-7600)

> Auto-generated by `scripts/gen-docs` from `tests/poc/_descriptions.json` + rule metadata. **Jangan edit di sini** — edit JSON-nya lalu jalankan `scripts/gen-docs`.

**Rule:** `rules/plaintext/known-cve.rules` · **Confidence:** high · **OWASP:** A06-Vulnerable-and-Outdated-Components · **Severity:** Critical · **Network:** plain

## Serangan apa ini?
Drupal Drupalgeddon2 (CVE-2018-7600): Drupal 7/8 Form API merender properti `#post_render`/`#markup` dari POST attacker → callback function (mis. `exec`/`system`) dipanggil dengan input penyerang.

## Kalau kena, dampaknya?
Remote Code Execution pre-auth — kampanye massal pasca-disclosure 2018, masih ke-scan rutin hingga sekarang.

## Apa yang rule ini deteksi?
URL `element_parents=account/mail/%23value` + body berisi `#post_render`/`#markup` yang nunjuk ke gadget eksekusi (exec/system/passthru/shell_exec).

## PoC vs Negative
- `poc.sh` → **harus** memicu SID 9300042.
- `negative.sh` → lookalike jinak; kena fast_pattern lalu **ditolak pcre** → TIDAK alert (§19 T6).

## Cara jalanin
```
scripts/rulectl check 9300042
```
