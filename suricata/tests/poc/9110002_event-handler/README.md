# SID 9110002 — WEB XSS event-handler injection (on*=) in http.uri

> Auto-generated by `scripts/gen-docs` from `tests/poc/_descriptions.json` + rule metadata. **Jangan edit di sini** — edit JSON-nya lalu jalankan `scripts/gen-docs`.

**Rule:** `rules/plaintext/xss.rules` · **Confidence:** medium · **OWASP:** A03-Injection · **Severity:** Major · **Network:** plain

## Serangan apa ini?
Reflected XSS via event-handler HTML (onerror=/onload=/onmouseover= dll) di parameter URL.

## Kalau kena, dampaknya?
Eksekusi JavaScript di browser korban.

## Apa yang rule ini deteksi?
Handler on*= dengan tanda = di URL; threshold 3/60 detik kompensator FP.

## PoC vs Negative
- `poc.sh` → **harus** memicu SID 9110002.
- `negative.sh` → lookalike jinak; kena fast_pattern lalu **ditolak pcre** → TIDAK alert (§19 T6).

## Cara jalanin
```
scripts/rulectl check 9110002
```
