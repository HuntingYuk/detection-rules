# SID 9600001 — WEB A02 JWT alg:none signature-strip token presented

> Auto-generated by `scripts/gen-docs` from `tests/poc/_descriptions.json` + rule metadata. **Jangan edit di sini** — edit JSON-nya lalu jalankan `scripts/gen-docs`.

**Rule:** `rules/plaintext/misc.rules` · **Confidence:** high · **OWASP:** A02-Cryptographic-Failures · **Severity:** Critical · **Network:** plain

## Serangan apa ini?
OWASP A02 — JWT `alg:none`: penyerang menanggalkan tanda tangan token JWT dan menyetel algoritma ke 'none'.

## Kalau kena, dampaknya?
Pemalsuan token = bypass autentikasi / privilege escalation.

## Apa yang rule ini deteksi?
Base64url blok-aligned dari header {"alg":"none → `eyJhbGciOiJub25l` pada token yang dikirim.

## PoC vs Negative
- `poc.sh` → **harus** memicu SID 9600001.
- `negative.sh` → lookalike jinak; kena fast_pattern lalu **ditolak pcre** → TIDAK alert (§19 T6).

## Cara jalanin
```
scripts/rulectl check 9600001
```
