# SID 9300047 — KNOWN-CVE Log4Shell obfuscated JNDI bypass (${::-} or ${env:...:-jndi}) in HTTP header (CVE-2021-44228)

> Auto-generated by `scripts/gen-docs` from `tests/poc/_descriptions.json` + rule metadata. **Jangan edit di sini** — edit JSON-nya lalu jalankan `scripts/gen-docs`.

**Rule:** `rules/plaintext/known-cve.rules` · **Confidence:** high · **OWASP:** A06-Vulnerable-and-Outdated-Components · **Severity:** Critical · **Network:** plain

## Serangan apa ini?
Log4Shell obfuscated JNDI bypass (CVE-2021-44228): rule 9300001 nangkep string literal `${jndi:`; varian real-world pakai trik substitusi `${::-j}${::-n}${::-d}${::-i}:` atau `${env:UNDEFINED:-jndi}:` yang di-resolve jadi `jndi:` saat log dipanggil — lolos matcher naive.

## Kalau kena, dampaknya?
Sama parahnya dengan Log4Shell base: Remote Code Execution pre-auth, tapi diam-diam lolos sensor yang cuma cek literal `${jndi:`.

## Apa yang rule ini deteksi?
Pola `${::-` di header HTTP diikuti substitusi huruf 'j' (mengarah ke 'jndi'), atau `${env:` + `:-jndi`. Sibling 9300001 untuk bare-string.

## PoC vs Negative
- `poc.sh` → **harus** memicu SID 9300047.
- `negative.sh` → lookalike jinak; kena fast_pattern lalu **ditolak pcre** → TIDAK alert (§19 T6).

## Cara jalanin
```
scripts/rulectl check 9300047
```
