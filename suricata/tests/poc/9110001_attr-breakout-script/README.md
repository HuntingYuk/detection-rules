# SID 9110001 — WEB XSS attribute-breakout into <script> in http.uri

> Auto-generated by `scripts/gen-docs` from `tests/poc/_descriptions.json` + rule metadata. **Jangan edit di sini** — edit JSON-nya lalu jalankan `scripts/gen-docs`.

**Rule:** `rules/plaintext/xss.rules` · **Confidence:** medium · **OWASP:** A03-Injection · **Severity:** Major · **Network:** plain

## Serangan apa ini?
Reflected XSS: breakout dari atribut HTML lalu inject `<script>` lewat parameter URL.

## Kalau kena, dampaknya?
Eksekusi JavaScript di browser korban (curi sesi/aksi atas nama korban).

## Apa yang rule ini deteksi?
Pola `"'>` breakout atribut tepat sebelum `<script` di URL (threshold 3/60 detik).

## PoC vs Negative
- `poc.sh` → **harus** memicu SID 9110001.
- `negative.sh` → lookalike jinak; kena fast_pattern lalu **ditolak pcre** → TIDAK alert (§19 T6).

## Cara jalanin
```
scripts/rulectl check 9110001
```
