# SID 9160006 — WEB Insecure Deserialization - Ruby YAML object injection (!ruby/object) in request body

> Auto-generated by `scripts/gen-docs` from `tests/poc/_descriptions.json` + rule metadata. **Jangan edit di sini** — edit JSON-nya lalu jalankan `scripts/gen-docs`.

**Rule:** `rules/plaintext/deserialization.rules` · **Confidence:** high · **OWASP:** A08-Software-and-Data-Integrity-Failures · **Severity:** Critical · **Network:** plain

## Serangan apa ini?
Insecure deserialization Ruby: YAML tidak aman (Psych) dengan tag `!ruby/object:` membangun objek Ruby arbitrer (Universal gadget).

## Kalau kena, dampaknya?
Remote Code Execution di server Ruby/Rails.

## Apa yang rule ini deteksi?
Tag Psych `!ruby/object|hash|struct|class:` di body request — tidak ada di data jinak.

## PoC vs Negative
- `poc.sh` → **harus** memicu SID 9160006.
- `negative.sh` → lookalike jinak; kena fast_pattern lalu **ditolak pcre** → TIDAK alert (§19 T6).

## Cara jalanin
```
scripts/rulectl check 9160006
```
