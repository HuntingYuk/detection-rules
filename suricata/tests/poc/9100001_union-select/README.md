# SID 9100001 — WEB SQLi UNION-based in http.uri (canonical)

> Auto-generated by `scripts/gen-docs` from `tests/poc/_descriptions.json` + rule metadata. **Jangan edit di sini** — edit JSON-nya lalu jalankan `scripts/gen-docs`.

**Rule:** `rules/plaintext/sqli.rules` · **Confidence:** medium · **OWASP:** A03-Injection · **Severity:** Major · **Network:** plain

## Serangan apa ini?
SQL injection UNION-based: penyerang menggabungkan query lewat `UNION SELECT` untuk menarik data dari tabel lain.

## Kalau kena, dampaknya?
Pencurian isi database (kredensial, data pelanggan).

## Apa yang rule ini deteksi?
`union select` di URL + struktur pcre; threshold 3/60 detik per-src sebagai kompensator FP.

## PoC vs Negative
- `poc.sh` → **harus** memicu SID 9100001.
- `negative.sh` → lookalike jinak; kena fast_pattern lalu **ditolak pcre** → TIDAK alert (§19 T6).

## Cara jalanin
```
scripts/rulectl check 9100001
```
