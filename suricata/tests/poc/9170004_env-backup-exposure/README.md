# SID 9170004 — WEB Exposure - /.env or config backup probe

> Auto-generated by `scripts/gen-docs` from `tests/poc/_descriptions.json` + rule metadata. **Jangan edit di sini** — edit JSON-nya lalu jalankan `scripts/gen-docs`.

**Rule:** `rules/plaintext/access-control.rules` · **Confidence:** high · **OWASP:** A05-Security-Misconfiguration · **Severity:** Major · **Network:** plain

## Serangan apa ini?
Penyerang menebak file konfigurasi rahasia (`.env`) atau backup-nya (`.env.bak`/`.env.local`/`.env.production`) yang nggak sengaja ke-publish ke web root.

## Kalau kena, dampaknya?
Bocornya seluruh secret aplikasi: kredensial DB, API key cloud, secret JWT, password mail SMTP — pintu masuk ke kompromi penuh.

## Apa yang rule ini deteksi?
Request ke path `/.env` atau backup-variantnya (`.env.bak/.backup/.local/.production/.prod/.dev/.old/.orig/.save/.swp`) — tidak ada klien sah yang minta dotfile env config.

## PoC vs Negative
- `poc.sh` → **harus** memicu SID 9170004.
- `negative.sh` → lookalike jinak; kena fast_pattern lalu **ditolak pcre** → TIDAK alert (§19 T6).

## Cara jalanin
```
scripts/rulectl check 9170004
```
