# SID 9500008 — PROTO-ANOMALY high TCP RST rate from one source - reset flood/scan

> Auto-generated by `scripts/gen-docs` from `tests/poc/_descriptions.json` + rule metadata. **Jangan edit di sini** — edit JSON-nya lalu jalankan `scripts/gen-docs`.

**Rule:** `rules/tls-meta/protocol-anomaly.rules` · **Confidence:** medium · **OWASP:** none · **Severity:** Major · **Network:** any

## Serangan apa ini?
Protocol anomaly: laju TCP RST tinggi dari satu sumber (RST-injection DoS / RST-scan / teardown spoof).

## Kalau kena, dampaknya?
DoS koneksi / pemindaian berbasis RST.

## Apa yang rule ini deteksi?
Laju TCP RST tinggi dari satu src (threshold 40/10 detik).

## PoC vs Negative
- `poc.sh` → **harus** memicu SID 9500008.
- `negative.sh` → lookalike jinak; kena fast_pattern lalu **ditolak pcre** → TIDAK alert (§19 T6).

## Cara jalanin
```
scripts/rulectl check 9500008
```
