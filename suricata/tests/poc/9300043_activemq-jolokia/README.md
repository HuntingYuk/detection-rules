# SID 9300043 — KNOWN-CVE Apache ActiveMQ Jolokia HTTP MBean exec RCE

> Auto-generated by `scripts/gen-docs` from `tests/poc/_descriptions.json` + rule metadata. **Jangan edit di sini** — edit JSON-nya lalu jalankan `scripts/gen-docs`.

**Rule:** `rules/plaintext/known-cve.rules` · **Confidence:** high · **OWASP:** A06-Vulnerable-and-Outdated-Components · **Severity:** Critical · **Network:** plain

## Serangan apa ini?
Apache ActiveMQ Jolokia HTTP MBean RCE: Jolokia agent expose JMX lewat HTTP `/api/jolokia`; request `exec` ke MBean `org.apache.activemq:type=Broker` membiarkan penyerang invoke method Java arbitrer (mis. menambah connector untuk pivoting).

## Kalau kena, dampaknya?
RCE / pivot lateral di broker ActiveMQ — dipakai operator ransomware bahkan saat port OpenWire (61616) di-firewall.

## Apa yang rule ini deteksi?
POST ke `/api/jolokia` + body JSON berisi `"type":"exec"` + MBean ActiveMQ Broker.

## PoC vs Negative
- `poc.sh` → **harus** memicu SID 9300043.
- `negative.sh` → lookalike jinak; kena fast_pattern lalu **ditolak pcre** → TIDAK alert (§19 T6).

## Cara jalanin
```
scripts/rulectl check 9300043
```
