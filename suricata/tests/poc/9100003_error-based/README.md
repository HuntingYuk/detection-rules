# SID 9100003 — WEB SQLi error-based (extractvalue/updatexml/exp) in http.uri

> Auto-generated by `scripts/gen-docs` from `tests/poc/_descriptions.json` + rule metadata. **Jangan edit di sini** — edit JSON-nya lalu jalankan `scripts/gen-docs`.

**Rule:** `rules/plaintext/sqli.rules` · **Confidence:** medium · **OWASP:** A03-Injection · **Severity:** Major · **Network:** plain

## Serangan apa ini?
SQL injection error-based: memaksa DB membocorkan data lewat pesan error (extractvalue/updatexml/exp).

## Kalau kena, dampaknya?
Pencurian data lewat pesan error DB.

## Apa yang rule ini deteksi?
Primitif error-extraction (extractvalue(/updatexml(/exp(~(select/convert(int,(select); threshold 3/60 detik.

## PoC vs Negative
- `poc.sh` → **harus** memicu SID 9100003.
- `negative.sh` → lookalike jinak; kena fast_pattern lalu **ditolak pcre** → TIDAK alert (§19 T6).

## Cara jalanin
```
scripts/rulectl check 9100003
```
