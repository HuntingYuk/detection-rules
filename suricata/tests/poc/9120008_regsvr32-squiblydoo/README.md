# SID 9120008 — WEB CMDi Windows regsvr32 Squiblydoo (scrobj.dll) remote scriptlet in http.uri

> Auto-generated by `scripts/gen-docs` from `tests/poc/_descriptions.json` + rule metadata. **Jangan edit di sini** — edit JSON-nya lalu jalankan `scripts/gen-docs`.

**Rule:** `rules/plaintext/cmdi.rules` · **Confidence:** high · **OWASP:** A03-Injection · **Severity:** Critical · **Network:** plain

## Serangan apa ini?
Squiblydoo: `regsvr32 /i:http(s)://.../p.sct scrobj.dll` — LOLBin Windows untuk mengeksekusi scriptlet (JS/VBS) remote, bypass banyak AV.

## Kalau kena, dampaknya?
Remote Code Execution dengan binary signed-Microsoft (defense evasion).

## Apa yang rule ini deteksi?
Kombinasi `regsvr32` + `/i:http(s)://` + `scrobj.dll` dalam jendela terbatas di URL.

## PoC vs Negative
- `poc.sh` → **harus** memicu SID 9120008.
- `negative.sh` → lookalike jinak; kena fast_pattern lalu **ditolak pcre** → TIDAK alert (§19 T6).

## Cara jalanin
```
scripts/rulectl check 9120008
```
