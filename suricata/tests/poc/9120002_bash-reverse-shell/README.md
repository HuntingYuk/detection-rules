# SID 9120002 — WEB CMDi bash reverse shell /dev/tcp/ in http.uri

> Auto-generated by `scripts/gen-docs` from `tests/poc/_descriptions.json` + rule metadata. **Jangan edit di sini** — edit JSON-nya lalu jalankan `scripts/gen-docs`.

**Rule:** `rules/plaintext/cmdi.rules` · **Confidence:** high · **OWASP:** A03-Injection · **Severity:** Critical · **Network:** plain

## Serangan apa ini?
Penyerang menyuntik idiom reverse-shell bash `/dev/tcp/<host>/<port>` lewat command injection agar server 'menelepon balik' ke mesin penyerang.

## Kalau kena, dampaknya?
Shell interaktif penuh ke server (Remote Code Execution) — kendali penuh.

## Apa yang rule ini deteksi?
Idiom `/dev/tcp/<host>/<port>` di URL — tidak pernah muncul di lalu lintas jinak.

## PoC vs Negative
- `poc.sh` → **harus** memicu SID 9120002.
- `negative.sh` → lookalike jinak; kena fast_pattern lalu **ditolak pcre** → TIDAK alert (§19 T6).

## Cara jalanin
```
scripts/rulectl check 9120002
```
