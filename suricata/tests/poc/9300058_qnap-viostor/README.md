# SID 9300058 — KNOWN-CVE QNAP Viostor server.cgi SPECIFIC_SERVER OS command injection (CVE-2023-47565)

> Auto-generated by `scripts/gen-docs` from `tests/poc/_descriptions.json` + rule metadata. **Jangan edit di sini** — edit JSON-nya lalu jalankan `scripts/gen-docs`.

**Rule:** `rules/plaintext/known-cve.rules` · **Confidence:** high · **OWASP:** A06-Vulnerable-and-Outdated-Components · **Severity:** Critical · **Network:** plain

## Serangan apa ini?
QNAP Viostor IP-camera unauth command injection (CVE-2023-47565): handler `server.cgi` accept parameter `SPECIFIC_SERVER=` di-pass langsung ke shell. 19 hit real-traffic di MTA scan-and-probe.

## Kalau kena, dampaknya?
RCE unauth di IP camera QNAP → privacy invasion + botnet recruit.

## Apa yang rule ini deteksi?
Path `/server.cgi` + parameter `SPECIFIC_SERVER=` berisi shell metachar (` $ | ;).

## PoC vs Negative
- `poc.sh` → **harus** memicu SID 9300058.
- `negative.sh` → lookalike jinak; kena fast_pattern lalu **ditolak pcre** → TIDAK alert (§19 T6).

## Cara jalanin
```
scripts/rulectl check 9300058
```
