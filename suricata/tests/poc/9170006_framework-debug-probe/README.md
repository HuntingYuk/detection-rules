# SID 9170006 — WEB Exposure - Symfony _profiler / Laravel _ignition / Rails /rails/info debug-page probe

> Auto-generated by `scripts/gen-docs` from `tests/poc/_descriptions.json` + rule metadata. **Jangan edit di sini** — edit JSON-nya lalu jalankan `scripts/gen-docs`.

**Rule:** `rules/plaintext/access-control.rules` · **Confidence:** high · **OWASP:** A05-Security-Misconfiguration · **Severity:** Major · **Network:** plain

## Serangan apa ini?
Penyerang nge-probe halaman debug framework yang nggak sengaja ke-expose ke production: Symfony Web Profiler `/_profiler`, Laravel Ignition `/_ignition/execute-solution` (CVE-2021-3129), Rails `/rails/info/routes`. Standar scanner recon.

## Kalau kena, dampaknya?
Bocornya routes/source-paths/DB-queries/env-vars → bahan recon serangan lanjutan; Ignition `execute-solution` = RCE pre-auth (Laravel debug).

## Apa yang rule ini deteksi?
Request ke salah satu canonical debug path (`/_profiler[/*]`, `/_ignition/execute-solution`, `/_ignition/health-check`, `/rails/info`, `/rails/info/properties`, `/rails/info/routes`).

## PoC vs Negative
- `poc.sh` → **harus** memicu SID 9170006.
- `negative.sh` → lookalike jinak; kena fast_pattern lalu **ditolak pcre** → TIDAK alert (§19 T6).

## Cara jalanin
```
scripts/rulectl check 9170006
```
