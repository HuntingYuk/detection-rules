# SID 9300034 — KNOWN-CVE Apache Struts2 REST plugin XStream deserialization RCE (CVE-2017-9805)

> Auto-generated by `scripts/gen-docs` from `tests/poc/_descriptions.json` + rule metadata. **Jangan edit di sini** — edit JSON-nya lalu jalankan `scripts/gen-docs`.

**Rule:** `rules/plaintext/known-cve.rules` · **Confidence:** high · **OWASP:** A08-Software-and-Data-Integrity-Failures · **Severity:** Critical · **Network:** plain

## Serangan apa ini?
Apache Struts2 REST plugin XStream deserialization RCE (CVE-2017-9805): body XML berisi gadget Nashorn (`jdk.nashorn.internal.objects.NativeString`) yang dieksekusi saat XStream men-deserialize.

## Kalau kena, dampaknya?
Remote Code Execution pre-auth di aplikasi Struts2 REST.

## Apa yang rule ini deteksi?
Marker class `jdk.nashorn.internal` (+ Nashorn-gadget atau `<dynamic-proxy>`) di body request XML.

## PoC vs Negative
- `poc.sh` → **harus** memicu SID 9300034.
- `negative.sh` → lookalike jinak; kena fast_pattern lalu **ditolak pcre** → TIDAK alert (§19 T6).

## Cara jalanin
```
scripts/rulectl check 9300034
```
