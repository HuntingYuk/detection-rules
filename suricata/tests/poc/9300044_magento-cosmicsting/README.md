# SID 9300044 — KNOWN-CVE Adobe Commerce / Magento CosmicSting XML SOAP deserialization (CVE-2024-34102)

> Auto-generated by `scripts/gen-docs` from `tests/poc/_descriptions.json` + rule metadata. **Jangan edit di sini** — edit JSON-nya lalu jalankan `scripts/gen-docs`.

**Rule:** `rules/plaintext/known-cve.rules` · **Confidence:** high · **OWASP:** A06-Vulnerable-and-Outdated-Components · **Severity:** Critical · **Network:** plain

## Serangan apa ini?
Adobe Commerce/Magento 'CosmicSting' (CVE-2024-34102): pre-auth XML SOAP deserialization — body JSON ke `/rest/V1/guest-carts/.../estimate-shipping-methods` di-embed XML DOCTYPE/xsi gadget yang men-deserialize lewat SimpleXMLElement → file read / RCE via PHP gadget chains.

## Kalau kena, dampaknya?
Pencurian file sensitif (Magento config/credentials) → eskalasi ke RCE penuh; dipakai kampanye penyerang mass-exploit toko e-commerce 2024.

## Apa yang rule ini deteksi?
POST ke `/rest/V1/guest-carts` + body JSON memuat marker SOAP-XML (`<!DOCTYPE`, `xmlns:xsi`, `SimpleXMLElement`, `xsi:type` atau namespace W3C XMLSchema) — dua buffer.

## PoC vs Negative
- `poc.sh` → **harus** memicu SID 9300044.
- `negative.sh` → lookalike jinak; kena fast_pattern lalu **ditolak pcre** → TIDAK alert (§19 T6).

## Cara jalanin
```
scripts/rulectl check 9300044
```
