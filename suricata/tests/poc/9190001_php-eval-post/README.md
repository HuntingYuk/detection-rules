# SID 9190001 — WEB Webshell PHP @eval($_POST ...) interaction (China Chopper-style)

> Auto-generated by `scripts/gen-docs` from `tests/poc/_descriptions.json` + rule metadata. **Jangan edit di sini** — edit JSON-nya lalu jalankan `scripts/gen-docs`.

**Rule:** `rules/plaintext/webshell.rules` · **Confidence:** high · **OWASP:** A03-Injection · **Severity:** Critical · **Network:** plain

## Serangan apa ini?
Webshell PHP one-liner China-Chopper: `@eval($_POST[...])` — eksekusi perintah dari variabel POST.

## Kalau kena, dampaknya?
Akses shell penuh & persisten ke server (pintu belakang).

## Apa yang rule ini deteksi?
Pola `@eval($_POST|REQUEST|GET` di body request.

## PoC vs Negative
- `poc.sh` → **harus** memicu SID 9190001.
- `negative.sh` → lookalike jinak; kena fast_pattern lalu **ditolak pcre** → TIDAK alert (§19 T6).

## Cara jalanin
```
scripts/rulectl check 9190001
```
