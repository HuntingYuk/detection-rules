# SID 9170005 — WEB Exposure - phpinfo() debug page probe

> Auto-generated by `scripts/gen-docs` from `tests/poc/_descriptions.json` + rule metadata. **Jangan edit di sini** — edit JSON-nya lalu jalankan `scripts/gen-docs`.

**Rule:** `rules/plaintext/access-control.rules` · **Confidence:** high · **OWASP:** A05-Security-Misconfiguration · **Severity:** Major · **Network:** plain

## Serangan apa ini?
Penyerang nge-probe halaman debug `phpinfo()` yang nggak sengaja ke-publish (`/phpinfo.php`, `/info.php`, `/test.php`, `/_profiler/phpinfo` Symfony). Standar scanner recon — 48 hit di 3 pcap MTA scan-and-probe.

## Kalau kena, dampaknya?
Bocornya konfigurasi PHP penuh: versi, ekstensi, environment vars (sering termasuk DB credentials, API keys, secret JWT), path absolut server — bahan recon untuk serangan lanjutan tepat sasaran.

## Apa yang rule ini deteksi?
Request ke salah satu path probe canonical phpinfo (`/phpinfo.php`/`/info.php`/`/test.php`/`/_profiler/phpinfo`/`/debug.php`).

## PoC vs Negative
- `poc.sh` → **harus** memicu SID 9170005.
- `negative.sh` → lookalike jinak; kena fast_pattern lalu **ditolak pcre** → TIDAK alert (§19 T6).

## Cara jalanin
```
scripts/rulectl check 9170005
```
