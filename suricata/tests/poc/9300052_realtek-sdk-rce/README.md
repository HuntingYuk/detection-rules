# SID 9300052 — KNOWN-CVE Realtek SDK formSysCmd RCE (CVE-2021-35394)

> Auto-generated by `scripts/gen-docs` from `tests/poc/_descriptions.json` + rule metadata. **Jangan edit di sini** — edit JSON-nya lalu jalankan `scripts/gen-docs`.

**Rule:** `rules/plaintext/known-cve.rules` · **Confidence:** high · **OWASP:** A06-Vulnerable-and-Outdated-Components · **Severity:** Critical · **Network:** plain

## Serangan apa ini?
Realtek RTL81xx/RTL83xx SDK command injection (CVE-2021-35394): SoC Realtek dipakai banyak vendor router/CPE small-brand; endpoint `boafrm/formSysCmd` accept parameter `sysCmd=` yang langsung di-shell-exec. Variant exploit mass-scanned via UDP (port 9034) dan HTTP — kita cover sisi HTTP-nya.

## Kalau kena, dampaknya?
RCE unauth di puluhan brand router kecil yang share Realtek SDK → botnet recruit, pivot LAN.

## Apa yang rule ini deteksi?
Path `/boafrm/formSysCmd` atau `/formSysCmd` + parameter `sysCmd=` berisi shell metachar (` $ | ;).

## PoC vs Negative
- `poc.sh` → **harus** memicu SID 9300052.
- `negative.sh` → lookalike jinak; kena fast_pattern lalu **ditolak pcre** → TIDAK alert (§19 T6).

## Cara jalanin
```
scripts/rulectl check 9300052
```
