# SID 9100006 — WEB SQLi Oracle out-of-band exfiltration via UTL_HTTP in http.uri

> Auto-generated by `scripts/gen-docs` from `tests/poc/_descriptions.json` + rule metadata. **Jangan edit di sini** — edit JSON-nya lalu jalankan `scripts/gen-docs`.

**Rule:** `rules/plaintext/sqli.rules` · **Confidence:** high · **OWASP:** A03-Injection · **Severity:** Critical · **Network:** plain

## Serangan apa ini?
SQL injection out-of-band Oracle: DB 'menelepon keluar' lewat `UTL_HTTP.request` membawa data hasil query.

## Kalau kena, dampaknya?
Pencurian isi database diam-diam walau hasil tak terlihat (blind/OOB).

## Apa yang rule ini deteksi?
Invokasi `UTL_HTTP.request(`/`.set_*` / `HTTPURITYPE(` di URL — high conf, tanpa threshold.

## PoC vs Negative
- `poc.sh` → **harus** memicu SID 9100006.
- `negative.sh` → lookalike jinak; kena fast_pattern lalu **ditolak pcre** → TIDAK alert (§19 T6).

## Cara jalanin
```
scripts/rulectl check 9100006
```
