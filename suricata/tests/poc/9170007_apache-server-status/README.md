# SID 9170007 — WEB Exposure - Apache mod_status /server-status /server-info probe

> Auto-generated by `scripts/gen-docs` from `tests/poc/_descriptions.json` + rule metadata. **Jangan edit di sini** — edit JSON-nya lalu jalankan `scripts/gen-docs`.

**Rule:** `rules/plaintext/access-control.rules` · **Confidence:** high · **OWASP:** A05-Security-Misconfiguration · **Severity:** Major · **Network:** plain

## Serangan apa ini?
Penyerang nge-probe halaman `/server-status` atau `/server-info` (Apache mod_status / mod_info) yang nggak sengaja aktif di production. Halaman ini ke-leak tabel worker process, URL request masuk, IP internal, config vhost.

## Kalau kena, dampaknya?
Bocornya struktur internal server Apache + traffic real-time (URL+IP) → bahan recon serangan lanjutan + privacy untuk klien.

## Apa yang rule ini deteksi?
Request ke `/server-status` atau `/server-info` (dengan/tanpa `?auto` atau `?refresh=N`).

## PoC vs Negative
- `poc.sh` → **harus** memicu SID 9170007.
- `negative.sh` → lookalike jinak; kena fast_pattern lalu **ditolak pcre** → TIDAK alert (§19 T6).

## Cara jalanin
```
scripts/rulectl check 9170007
```
