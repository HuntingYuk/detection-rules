# SID 9160004 — WEB Insecure Deserialization - Python YAML object injection (!!python/object) in request body

> Auto-generated by `scripts/gen-docs` from `tests/poc/_descriptions.json` + rule metadata. **Jangan edit di sini** — edit JSON-nya lalu jalankan `scripts/gen-docs`.

**Rule:** `rules/plaintext/deserialization.rules` · **Confidence:** high · **OWASP:** A08-Software-and-Data-Integrity-Failures · **Severity:** Critical · **Network:** plain

## Serangan apa ini?
Insecure deserialization Python: `yaml.load` tidak aman dengan tag `!!python/object/apply` untuk membangun objek arbitrer.

## Kalau kena, dampaknya?
Remote Code Execution di server Python.

## Apa yang rule ini deteksi?
Tag PyYAML `!!python/object|module|apply` di body request — tidak ada di data YAML/JSON jinak.

## PoC vs Negative
- `poc.sh` → **harus** memicu SID 9160004.
- `negative.sh` → lookalike jinak; kena fast_pattern lalu **ditolak pcre** → TIDAK alert (§19 T6).

## Cara jalanin
```
scripts/rulectl check 9160004
```
