# SID 9180002 — WEB Auth brute-force - high Basic-Auth header rate from one source

> Auto-generated by `scripts/gen-docs` from `tests/poc/_descriptions.json` + rule metadata. **Jangan edit di sini** — edit JSON-nya lalu jalankan `scripts/gen-docs`.

**Rule:** `rules/plaintext/auth-bruteforce.rules` · **Confidence:** medium · **OWASP:** A07-Identification-and-Authentication-Failures · **Severity:** Major · **Network:** plain

## Serangan apa ini?
Penyerang spray kombinasi `username:password` lewat header `Authorization: Basic` ke endpoint berproteksi (login HTTP Basic) — varian credential-stuffing yang nggak lewat form login.

## Kalau kena, dampaknya?
Akun jebol kalau ada password lemah/bocor; bisa juga DoS layanan auth.

## Apa yang rule ini deteksi?
Laju header `Authorization: Basic` tinggi dari satu IP (threshold 30/60 detik) — sibling 9180001 (POST login form).

## PoC vs Negative
- `poc.sh` → **harus** memicu SID 9180002.
- `negative.sh` → lookalike jinak; kena fast_pattern lalu **ditolak pcre** → TIDAK alert (§19 T6).

## Cara jalanin
```
scripts/rulectl check 9180002
```
