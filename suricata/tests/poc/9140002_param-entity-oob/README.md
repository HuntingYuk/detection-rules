# SID 9140002 — WEB XXE parameter-entity OOB (<!ENTITY % .. SYSTEM) in request body

> Auto-generated by `scripts/gen-docs` from `tests/poc/_descriptions.json` + rule metadata. **Jangan edit di sini** — edit JSON-nya lalu jalankan `scripts/gen-docs`.

**Rule:** `rules/plaintext/xxe.rules` · **Confidence:** high · **OWASP:** A03-Injection · **Severity:** Critical · **Network:** plain

## Serangan apa ini?
XXE blind out-of-band: parameter-entity eksternal (`<!ENTITY % ... SYSTEM>`) untuk eksfiltrasi via DTD eksternal.

## Kalau kena, dampaknya?
Eksfiltrasi file server walau respons tak menampilkan hasil.

## Apa yang rule ini deteksi?
`<!ENTITY %` yang SYSTEM (fetch DTD eksternal) di body request.

## PoC vs Negative
- `poc.sh` → **harus** memicu SID 9140002.
- `negative.sh` → lookalike jinak; kena fast_pattern lalu **ditolak pcre** → TIDAK alert (§19 T6).

## Cara jalanin
```
scripts/rulectl check 9140002
```
