# SID 9130005 — WEB Path Traversal Unicode overlong-UTF-8 bypass (%c0%af/%c1%9c) in http.uri.raw

> Auto-generated by `scripts/gen-docs` from `tests/poc/_descriptions.json` + rule metadata. **Jangan edit di sini** — edit JSON-nya lalu jalankan `scripts/gen-docs`.

**Rule:** `rules/plaintext/traversal.rules` · **Confidence:** high · **OWASP:** A03-Injection · **Severity:** Critical · **Network:** plain

## Serangan apa ini?
Path traversal evasion: penyerang pakai encoding UTF-8 overlong `%c0%af` (encoding INVALID per RFC 3629 untuk '/') agar filter yang cuma blokir `%2f` lewat.

## Kalau kena, dampaknya?
Pembacaan file sensitif (LFI) lewat bypass filter sederhana — pijakan recon/kompromi.

## Apa yang rule ini deteksi?
Byte sequence `%c0%af` (atau `%c1%9c` untuk '\\') dalam konteks traversal (didahului `..` atau diikuti `etc/`/`windows`/`passwd`/`win.ini`).

## PoC vs Negative
- `poc.sh` → **harus** memicu SID 9130005.
- `negative.sh` → lookalike jinak; kena fast_pattern lalu **ditolak pcre** → TIDAK alert (§19 T6).

## Cara jalanin
```
scripts/rulectl check 9130005
```
