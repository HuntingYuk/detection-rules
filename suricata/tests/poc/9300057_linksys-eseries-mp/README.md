# SID 9300057 — KNOWN-CVE Linksys E-Series mp parameter OS command injection (CVE-2025-34037)

> Auto-generated by `scripts/gen-docs` from `tests/poc/_descriptions.json` + rule metadata. **Jangan edit di sini** — edit JSON-nya lalu jalankan `scripts/gen-docs`.

**Rule:** `rules/plaintext/known-cve.rules` · **Confidence:** high · **OWASP:** A06-Vulnerable-and-Outdated-Components · **Severity:** Critical · **Network:** plain

## Serangan apa ini?
Linksys E-Series OS command injection lewat parameter `mp` CGI (CVE-2025-34037): handler `/Diagnostics.tri` atau `/set_TR069` menerima parameter `mp=` yang langsung di-shell-exec. Shape mirip dipakai Belkin/Tuoshi/Edimax/Tenda — multi-vendor lewat shared SDK.

## Kalau kena, dampaknya?
RCE unauth di router Linksys (dan OEM clones) → botnet recruit + pivot LAN.

## Apa yang rule ini deteksi?
Path Linksys-style CGI (`Diagnostics.tri`/`set_TR069`/`cgi-bin`/`goform`) + parameter `mp=` berisi shell metachar (` $ | ;).

## PoC vs Negative
- `poc.sh` → **harus** memicu SID 9300057.
- `negative.sh` → lookalike jinak; kena fast_pattern lalu **ditolak pcre** → TIDAK alert (§19 T6).

## Cara jalanin
```
scripts/rulectl check 9300057
```
