# SID 9170003 — WEB Exposure - /.svn/wc.db SVN repo probe

> Auto-generated by `scripts/gen-docs` from `tests/poc/_descriptions.json` + rule metadata. **Jangan edit di sini** — edit JSON-nya lalu jalankan `scripts/gen-docs`.

**Rule:** `rules/plaintext/access-control.rules` · **Confidence:** high · **OWASP:** A05-Security-Misconfiguration · **Severity:** Major · **Network:** plain

## Serangan apa ini?
Penyerang menebak path file internal SVN yang nggak sengaja ke-publish ke web root, yaitu `/.svn/wc.db` (working-copy database).

## Kalau kena, dampaknya?
Bocornya seluruh source code + history versi-control + secret yang pernah ke-commit (analog ke kebocoran `.git`).

## Apa yang rule ini deteksi?
Request ke path `/.svn/wc.db` — tidak ada klien sah yang minta file ini.

## PoC vs Negative
- `poc.sh` → **harus** memicu SID 9170003.
- `negative.sh` → lookalike jinak; kena fast_pattern lalu **ditolak pcre** → TIDAK alert (§19 T6).

## Cara jalanin
```
scripts/rulectl check 9170003
```
