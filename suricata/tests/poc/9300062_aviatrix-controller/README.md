# SID 9300062 — KNOWN-CVE Aviatrix Controller CloudxaaS pre-auth RCE (CVE-2024-50603)

> Auto-generated by `scripts/gen-docs` from `tests/poc/_descriptions.json` + rule metadata. **Jangan edit di sini** — edit JSON-nya lalu jalankan `scripts/gen-docs`.

**Rule:** `rules/plaintext/known-cve.rules` · **Confidence:** high · **OWASP:** A06-Vulnerable-and-Outdated-Components · **Severity:** Critical · **Network:** plain

## Serangan apa ini?
Aviatrix Controller pre-auth OS command injection (CVE-2024-50603): endpoint `/v1/api` accept field `cloud_type`/`account_password`/`account_name` yang langsung di-concat ke shell call. Dieksploitasi 2025 oleh operator ransomware buat pivot dari cloud-mgmt console ke seluruh fleet.

## Kalau kena, dampaknya?
RCE pre-auth di Aviatrix Controller → akses penuh ke cloud-network management dari banyak akun cloud sekaligus (high blast-radius).

## Apa yang rule ini deteksi?
POST ke `/v1/api` + field cloud_type/account_password/account_name/aws_iam_user_name/gcloud_project_credentials_filename berisi shell metachar (` $ | ;) — dua buffer.

## PoC vs Negative
- `poc.sh` → **harus** memicu SID 9300062.
- `negative.sh` → lookalike jinak; kena fast_pattern lalu **ditolak pcre** → TIDAK alert (§19 T6).

## Cara jalanin
```
scripts/rulectl check 9300062
```
