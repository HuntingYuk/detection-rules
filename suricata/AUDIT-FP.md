# AUDIT-FP.md — broad-benign false-positive probe

> Auto-generated by `scripts/audit-fp` — runs ~64 realistic benign scenarios (docs-with-attack-terms, response-leak-prose, exploit-shaped endpoints, below-threshold volume, normal protocol, cookie overlaps) through the full ruleset. ANY firing alert = an FP candidate.

## Headline

- Benign scenarios: **64**
- Scenarios that ALERTED (FP candidates): **2**
- Scenarios that stayed silent (clean): 62
- Distinct rules with at least one FP: **2**

## 🟡 FP candidates — rules to review

Each row: rule SID that fired on a *benign* scenario. Either tighten the rule's pcre to exclude this pattern, or document the residual as a known tune-target (threshold by dst).

| Rule SID | Rule msg | Fired on benign scenario(s) |
|----------|----------|----------------------------|
| 9150003 | WEB SSRF to GCP cloud metadata hostname metadata.google.internal in http.uri [hi | docs_metadata_gcp |
| 9210002 | WEB Info Leak - PEM private key in HTTP response body [high] | docs_resp_pem |

## Detail — per benign scenario that alerted

- **docs_metadata_gcp** → SIDs: 9150003
- **docs_resp_pem** → SIDs: 9210002

---

**Method.** Each scenario is one TCP/HTTP/UDP flow; client IPs are rewritten to unique 10.99.x.y for per-flow attribution. L4 checksums are recomputed and `suricata -k none` ignores stale ones. Full ruleset loaded ONCE. Re-run after rule edits to confirm the corpus stays FP-clean against broad benign traffic.
